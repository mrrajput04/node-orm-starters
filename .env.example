# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=orm_templates_db

# MongoDB
MONGO_URI=mongodb://localhost:27017/orm_templates_db

# Prisma
DATABASE_URL="mysql://root:@localhost:3306/orm_templates_db"

# Application
NODE_ENV=development
PORT=3000

# JWT Secret (if needed for authentication)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Optional: Different database for each ORM
SEQUELIZE_DB=sequelize_db
MONGOOSE_DB=mongoose_db
TYPEORM_DB=typeorm_db
PRISMA_DB=prisma_db
KNEX_DB=knex_db
OBJECTION_DB=objection_db
MIKROORM_DB=mikroorm_db

# Optional: Redis (for caching)
REDIS_URL=redis://localhost:6379

# Optional: Email configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password